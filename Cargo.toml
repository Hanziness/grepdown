[workspace]
resolver = "3"
members = ["crates/*"]

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
strip = true
panic = "abort"        # saves unwinding tables; fine for a CLI that exits on error anyway